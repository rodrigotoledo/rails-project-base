default: &default
  adapter: postgresql
  encoding: unicode
  host: db
  username: postgres
  password: password
  pool: 5

development:
  <<: *default
  database: project_name_development

test:
  <<: *default
  database: project_name_test

production:
  url: <%= ENV['DATABASE_URL'] %>
